#pragma once
#include "matrix.h"

namespace llm
{
	/*class IAlive abstract
	{
	public:
		virtual Matrix forward(const Matrix& input) = 0;
		virtual Matrix backward(Matrix const& dOut) = 0;
	protected:
	};

	class Layer abstract
		:public IAlive
	{
	public:

	protected:
		Matrix m_LastInput;
	};*/
}